Configuration:
===============

    [plugin:jira]
    server = https://jira.example.com
    user = jira user
    password = jirapassword
    project = PROJECT
    field_ip = IP
    field_mac = MAC
    field_hostname = FQDN
